<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:versionCode="12"
	android:versionName="1.2">

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application
		android:name="com.resort_cloud.nansei.nansei_tablet.NavigationApplication"
		android:allowBackup="true"
		android:configChanges="locale"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name_nansei"
		android:largeHeap="true"
		android:requestLegacyExternalStorage="true"
		android:restoreAnyVersion="true"
		android:supportsRtl="true"
		android:theme="@style/Theme.OsmAndLibrarysample">

		<provider
			android:name="androidx.core.content.FileProvider"
			android:authorities="net.osmand.library.nansei_tablet.fileprovider"
			android:exported="false"
			android:grantUriPermissions="true">
			<meta-data
				android:name="android.support.FILE_PROVIDER_PATHS"
				android:resource="@xml/paths" />
		</provider>

		<activity
			android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
				<category android:name="android.intent.category.MULTIWINDOW_LAUNCHER" />
			</intent-filter>
		</activity>

		<!--used for restart after switch between opengl and legacy renderer-->
		<activity
			android:name="net.osmand.plus.activities.RestartActivity"
			android:exported="false"
			android:process=":restart"
			android:theme="@android:style/Theme.Translucent.NoTitleBar" />

		<!--used for maps downloading-->
		<activity android:name="net.osmand.plus.download.DownloadActivity" />

	</application>

</manifest>