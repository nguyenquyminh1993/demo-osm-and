package net.osmand.library.sample;

import net.osmand.plus.OsmandApplication;

// via SampleApplication we get access to OsmandApplication and all its settings and helpers
public class NavigationApplication extends OsmandApplication {
	@Override
	public void onCreate() {
		super.onCreate();
	}
}
